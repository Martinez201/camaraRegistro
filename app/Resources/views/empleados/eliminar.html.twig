{% extends 'navegacion.html.twig' %}

{% block title %}

    Eliminar  Empleado

{% endblock %}

{% block content %}

    <h1>Eliminar Empleado</h1>

    <div class="alert alert-warning">
        ¿Esta seguro de que quiere eliminar el empleado {{ empleados.id }}?  Esta operación no se puede deshacer.
    </div>

    <form method="post">
        <a href="{{ path('empleados_form',{'id': empleados.id}) }}" class="btn btn-info">Volver atrás</a>
        <button type="submit" class="btn btn-danger">Borrar</button>
    </form>
{% endblock %}

